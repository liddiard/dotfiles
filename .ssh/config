# create or attach to existing tmux session
Host smarthome
 User pi
 RemoteCommand tmux new -A -s default
 RequestTTY yes

# create or attach to existing tmux session
Host matrix
 User pi
 RemoteCommand tmux new -A -s default
 RequestTTY yes

# create or attach to existing tmux session
Host webserver
 User pi
 RemoteCommand tmux new -A -s default
 RequestTTY yes
