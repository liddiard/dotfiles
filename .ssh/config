# create or attach to existing tmux session
Host pi
 RemoteCommand tmux new -A -s default
 RequestTTY yes

# create or attach to existing tmux session
Host matrix
 RemoteCommand tmux new -A -s default
 RequestTTY yes
